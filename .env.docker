# Healthcare Security Research Platform - Docker Environment Configuration
# Copy this file to .env and update with your values

# Database Configuration
# Note: In Docker, DB_HOST is set to 'postgres' (the container name) in docker-compose.yml
# DB_NAME is set to 'healthcare_security' in docker-compose.yml
DB_PASSWORD=healthcare_secure_password_123
DB_PORT=5432

# LLM Service Configuration
LLM_PORT=11434
LLM_MODEL=llama3.1-sql:latest

# Backend Configuration
BACKEND_HOST=localhost
BACKEND_PORT=5000
FLASK_ENV=development
FLASK_DEBUG=False

# Frontend Configuration
FRONTEND_PORT=5173

# Documentation Port (optional)
DOCS_PORT=8080

# Security Configuration
SECURITY_MODE=vulnerable
SECRET_KEY=GENERATE_A_SECURE_RANDOM_KEY_HERE
JWT_SECRET_KEY=GENERATE_A_SECURE_RANDOM_JWT_KEY_HERE

# Logging
LOG_LEVEL=INFO

# CORS (use * for development, specific origins for production)
CORS_ORIGINS=*

# Production Settings (uncomment for production)
# FLASK_ENV=production
# SECURITY_MODE=secure
# CORS_ORIGINS=https://yourdomain.com
# LOG_LEVEL=WARNING
